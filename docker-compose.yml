version: '3.9'
services:
  web:
    build: .
    container_name: consgenerate_web
    ports:
      - "8000:8000"
    volumes:
      - ./config:/app/config
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
